-- Divides two numbers

DELIMITER $$
CREATE FUNCTION SafeDiv (a INT, b INT)
RETURNS DEC(32, 24)
DETERMINISTIC
BEGIN
	IF (b = 0)
		THEN
		RETURN 0;
	ELSE
		RETURN CAST(a AS DEC(32, 24)) / b;
	END IF;
END $$
DELIMITER $$
